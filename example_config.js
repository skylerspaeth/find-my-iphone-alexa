module.exports = {
    // Account information to authenticate with iCloud
    username: 'user@domain.com',
    password: 'P@55w0rd',

    // Address to check distance of device from (no formatting characters)
    address: '1234 Amazon Blvd Austin TX',

    // Units when calculating distance: only 'km' or 'mi' for now
    distanceUnits: 'km'
};
