service: aws-node-alexa-skill

frameworkVersion: ">=1.4.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs4.3

functions:
  findMyIphone:
    handler: handler.findMyIphone
    events:
      - alexaSkill
